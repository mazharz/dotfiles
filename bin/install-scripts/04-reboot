#!/bin/bash

echo '########################################################################'
echo '# We are about to reboot, after that, make sure you have your normal'
echo '# user and run the rest of the scripts via that user.'
echo '########################################################################'
echo -n '# type "y" to continue: '

read -r answer

if [ "$answer" == "y" ]; then
  reboot
fi

